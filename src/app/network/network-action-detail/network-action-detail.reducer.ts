const initialState: any = {
}

export function reducer(state = initialState, action) {
    switch (action.type) {

        case 'NETWORK_ACTION_DETAIL_LOAD': {
            return {
                
            }
        }

        default:
            return state;
    }
}